[package]
name = "ttf"
version = "0.1.0"
edition = "2024"
description = "Terminal Tool Finder — fuzzy search terminal commands from tools.json"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
